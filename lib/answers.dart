import 'package:flutter/material.dart';

class Answers extends StatelessWidget {
  Answers({required this.listOfWidgets, Key? key}) : super(key: key);

  final List<Widget> listOfWidgets;

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
